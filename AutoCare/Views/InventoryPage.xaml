<Page x:Class="AutoCare.Views.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AutoCare.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InventoryPage"
      Background="MintCream">

    <Page.Resources>

        <Style x:Key="popupStyle" TargetType="{x:Type ColumnDefinition}">
            <Style.Triggers>
                <Trigger Property="{Binding ShowSide}" Value="true">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetProperty="Width"
                                    From="0" To="200"
                                    Duration="0:0:0.20"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"  Grid.Row="0" HorizontalAlignment="Right">
            <Button Content="Add Item" Click="Button_Click"  Padding="3" Margin="5"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="tbSearch" FontSize="14" Margin="0,0,10,0" VerticalContentAlignment="Center" Grid.Column="0" Padding="3" />
            <Button Width="40" Height="30" VerticalAlignment="Center"  Grid.Column="1" Click="Button_Click_1">
                <Image Width="20" Height="20" Source="/Assets/img.png" />
            </Button>
        </Grid>

        <Grid x:Name="gr" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer x:Name="SlidingPanel" Grid.Column="1" VerticalScrollBarVisibility="Auto"  Width="0">
                <local:AddUpdateUser x:Name="aus" AddUserClick="aus_AddUserClick" />
            </ScrollViewer>

            <TabControl Grid.Column="0">
                <TabItem Header="Classic">
                    <DataGrid ItemsSource="{Binding FilteredItems, Mode=OneWay}" CanUserAddRows="False" IsReadOnly="True" />
                </TabItem>
                <TabItem Header="Custom">
                    <ListView ItemsSource="{Binding FilteredItems, Mode=OneWay}" HorizontalContentAlignment="Stretch">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="auto" />
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Vertical" Grid.Column="0">
                                            <TextBlock>
                                                <TextBlock.Inlines>
                                                    <Run Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="DarkSlateGray"/>
                                                    <Run Text="{Binding Location, StringFormat=' - [{0}]'}" />
                                                </TextBlock.Inlines>
                                            </TextBlock>

                                            <TextBlock Text="{Binding Description}"  MaxWidth="250" TextWrapping="Wrap" Margin="0,5,10,0" Foreground="DimGray" HorizontalAlignment="Left" />
                                        </StackPanel>

                                        <StackPanel Orientation="Vertical" Margin="10 0 0 0" Grid.Column="1">
                                            <TextBlock Text="Buying Price" FontWeight="Bold" />
                                            <TextBlock Text="{Binding BuyingPrice, StringFormat='₨{0:N2}'}"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Vertical" Margin="10 0 0 0" Grid.Column="2">
                                            <TextBlock Text="Selling Price" FontWeight="Bold" />
                                            <TextBlock Text="{Binding SellingPrice, StringFormat='₨{0:N2}'}"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Vertical" Margin="10 0 0 0" Grid.Column="3">
                                            <TextBlock Text="Stock Quantity" FontWeight="Bold" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding StockQuantity}"/>
                                        </StackPanel>


                                        <StackPanel Orientation="Vertical" Margin="10 0 0 0" Grid.Column="4">
                                            <TextBlock Text="Sold Quantity" FontWeight="Bold" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding SoldQuantity}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Page>
